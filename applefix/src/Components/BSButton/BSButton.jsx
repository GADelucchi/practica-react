// Imports
import Button from 'react-bootstrap/Button';
import CartWidget from '../CartWidget'

// Code
const BSButton = () => {
    return (
            <Button variant="dark">
                <CartWidget />
            </Button>
    );
}

// Exports
export default BSButton;